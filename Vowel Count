//https://www.codewars.com/kata/54ff3102c1bad923760001f3/train/cpp

int getCount(const string& inputStr){
  int num_vowels = 0;
  for (auto c : inputStr)
    {
    if  (c == 'a' || c == 'e' || c == 'o' || c == 'i' || c == 'u') num_vowels++;
  }
  return num_vowels;
